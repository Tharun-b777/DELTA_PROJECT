
user nginx;

events {
    worker_connections   1000;
}
http {
        upstream iapi{
  
  server api:5000;
}
        server {
              listen 80;
              location / {
                proxy_pass http://iapi;
              }
        }
}
